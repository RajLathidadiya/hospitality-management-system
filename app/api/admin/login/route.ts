import {NextResponse} from 'next/server'; import {adminCredentials,makeToken} from '@/lib/auth';
export async function POST(req:Request){const b=await req.json();const c=adminCredentials();if(b.email!==c.email||b.password!==c.password)return NextResponse.json({error:'Invalid email or password'},{status:401});const r=NextResponse.json({ok:true});r.cookies.set('sawariya_admin',makeToken(c.email),{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',path:'/',maxAge:604800});return r}
