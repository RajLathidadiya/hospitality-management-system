'use client';
import {FormEvent,useState} from 'react';
export default function AdminLogin(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);
async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});if(r.ok)window.location.href='/admin';else{const d=await r.json();setError(d.error||'Login failed')}setLoading(false)}
return <main className="admin-auth"><div className="admin-auth-card"><img src="/sawariya-logo.jpeg" className="admin-auth-logo" alt="Sawariya Event"/><p className="admin-kicker">ADMIN PORTAL</p><h1>Welcome back</h1><p className="admin-muted">Manage properties, events and inquiries from one place.</p><form className="admin-form" onSubmit={submit} autoComplete="off"><label>Email<input name="admin-login-email" type="email" autoComplete="off" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input name="admin-login-password" type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="admin-error">{error}</p>}<button className="admin-primary" disabled={loading}>{loading?'Signing in…':'Sign in'}</button></form><p className="admin-demo-note">Local admin credentials can be changed with ADMIN_EMAIL and ADMIN_PASSWORD in .env.local.</p><a href="/" className="admin-back">← Back to website</a></div></main>}
