'use client';
import { FormEvent, useState } from 'react';
export default function SuperAdminLogin() {
  const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false);
  async function submit(e: FormEvent) { e.preventDefault(); setLoading(true); setError(''); const r = await fetch('/api/superadmin/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email, password }) }); if (r.ok) window.location.href = '/superadmin'; else { const d = await r.json().catch(() => ({})); setError(d.error || 'Login failed'); setLoading(false); } }
  return <main className="admin-auth"><div className="admin-auth-card"><img src="/sawariya-logo.jpeg" className="admin-auth-logo" alt="Sawariya Event"/><p className="admin-kicker">SUPERADMIN PORTAL</p><h1>Approval Center</h1><p className="admin-muted">Review and approve content before it becomes visible on the website.</p><form className="admin-form" onSubmit={submit}><label>Email<input type="email" value={email} onChange={e => setEmail(e.target.value)} required autoComplete="username" /></label><label>Password<input type="password" value={password} onChange={e => setPassword(e.target.value)} required autoComplete="current-password" /></label>{error && <p className="admin-error">{error}</p>}<button className="admin-primary" disabled={loading}>{loading ? 'Signing in…' : 'Sign in as Superadmin'}</button></form><a href="/admin/login" className="admin-back">← Admin login</a></div></main>;
}
